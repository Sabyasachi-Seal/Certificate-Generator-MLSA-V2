[project]
name = "certificate-generator-mlsa"
version = "0.1.0"
description = "MLSA Certificate Generator – serverless PDF & email on Vercel"
requires-python = ">=3.12"
dependencies = [
    "xhtml2pdf>=0.2.16",
    "resend>=2.0.0",
]

[tool.vercel]
entrypoint = "api.send:handler"

